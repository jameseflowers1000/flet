"""
ResizablePanel - Client-side resizable splitter for Flet.

The Python control is defined inline in epyx.core.splitter to avoid
requiring a separate pip-installed package. This package exists only
to house the Flutter/Dart widget code.
"""

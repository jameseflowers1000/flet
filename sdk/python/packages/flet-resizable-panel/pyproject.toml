[project]
name = "flet-resizable-panel"
version = "0.1.0"
description = "Client-side resizable panel splitter for Flet - 60fps drag with zero WebSocket traffic"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "Flet contributors", email = "hello@flet.dev" }
]
dependencies = [
    "flet",
]

[dependency-groups]
dev = [
    "flet[all]",
    "pytest",
]

[project.urls]
Homepage = "https://flet.dev"
Repository = "https://github.com/flet-dev/flet"

[tool.setuptools.package-data]
"flutter.flet_resizable_panel" = ["**/*"]

[tool.setuptools]
license-files = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

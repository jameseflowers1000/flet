/// Non-web stub for DOM mirror helpers. The real implementation lives in
/// dom_helpers_web.dart and is selected via conditional import based on
/// `dart.library.js_interop` availability.
void publishBanner(String message, String level) {}
void publishBeep() {}

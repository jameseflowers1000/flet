[project]
name = "flet-marching-ants"
version = "0.1.0"
description = "Animated dashed-border (marching-ants) wrapper for Flet"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "Flet contributors", email = "hello@flet.dev" }
]
dependencies = [
    "flet",
]

[dependency-groups]
dev = [
    "flet[all]",
    "pytest",
]

[project.urls]
Homepage = "https://flet.dev"
Repository = "https://github.com/flet-dev/flet"

[tool.setuptools.package-data]
"flutter.flet_marching_ants" = ["**/*"]

[tool.setuptools]
license-files = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
